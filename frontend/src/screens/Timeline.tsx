import { Stub } from '../components/Stub';
import { IconTimeline } from '../components/icons';

export function Timeline() {
  return (
    <Stub
      icon={IconTimeline}
      title="Timeline — coming soon"
      note="A single stream of what your machines observed: commits, sessions, notes."
    />
  );
}
