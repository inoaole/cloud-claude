{
  "name": "cloud-claude-hub",
  "private": true,
  "version": "0.9.0",
  "description": "cloud-claude hub — single Node process: serves the PWA + terminal/agent relay + collector ingest → SQLite timeline",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  }
}
