{
  "name": "cloud-claude-hub",
  "private": true,
  "version": "0.2.0",
  "description": "cloud-claude hub — single Node process: serves the PWA and (later) collector ingest + terminal relay",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
