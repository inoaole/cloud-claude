# Commit Log — 2026-07-02 · README

- `docs(readme): public-facing README (two planes, security model, stack, layout)`
  The repo goes public later as a portfolio piece — this is its front door. Covers the
  personal-OS pitch, the two-plane architecture (ASCII), the honesty-as-a-feature status
  language, the security model (tailnet-only, split credential paths, single-use WS tokens,
  spawn-no-shell), stack rationale, repo layout, and pointers to deploy/collector docs.
  Deliberately NO tailnet hostnames/IPs/PIN — placeholders and gitignored-config pointers only.
