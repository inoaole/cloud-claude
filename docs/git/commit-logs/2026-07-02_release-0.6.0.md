# Commit Log — 2026-07-02 · release 0.6.0

- `chore(release): bump to 0.6.0 (v0.6 agent mode merged to develop)`
  PR #6 merged to develop: the Machines device console — Agent (claude stream-json over ssh) +
  Shell (command blocks) + Terminal (raw pty), session list/New/Kill, keep-alive reattach, Stop.
  VERSION 0.5.0 → 0.6.0 + hub/package.json + frontend/package.json. Verified e2e on MacBook Pro
  and the Ubuntu hub. Deferred: diff sheet, device health probe, reconnect banner, Terminal demotion.
