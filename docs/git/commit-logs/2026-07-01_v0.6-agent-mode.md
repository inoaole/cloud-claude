# Commit Log — 2026-07-01 · v0.6 Agent mode (feature/v0.6.0/agent-mode)

Builds on the command-chat foundation (two-mode DeviceConsole + shell CommandChat). Adds the
Agent mode: chat with claude on a device, structured. Plan + T0 spike + reviews:
`~/.gstack/projects/cloud-claude/a1234-v0.6-agent-mode-plan-20260701.md` (ENG + DESIGN CLEARED,
T0 SSH spike PASSED).

- `feat(v0.6/T1): agent.js — claude stream-json relay core (mapEvent + command builder)`
  The IO-free, tested core of Agent mode. `mapEvent` translates claude's stream-json JSONL →
  phone WS events (assistant text / thinking / tool_use / tool_result / result / status /
  assistant_delta), mapped against the real event shapes captured in the T0 spike
  (designs/agent-spike-20260701) — never throws on a malformed event (codex #17). `buildAgentCommand`
  builds the `ssh -tt … setsid bash -lc 'cd <cwd> && exec claude -p --input-format stream-json
  --output-format stream-json --include-partial-messages --permission-mode acceptEdits'` command:
  `-tt` so SIGINT/Stop propagates (D4), `setsid` so killing ssh reaps claude + children (codex #7),
  cwd validated by `assertCwd` (absolute, no traversal/metachars, optional allowedRoots — codex
  #5/#14), device fields guarded by `assertSafeArg`. `wrapUserMessage` = the verified stdin schema.
  Tests: hub `node --test` +14 (38 total) — event mapping for every block type + malformed input,
  cwd guard, command shape for Mac/hub. claude runs ON the device (its own auth); hub only pipes.

- `feat(v0.6/T2): agent session registry + /sessions routes`
  `hub/src/sessions.js` — agent-only in-memory registry (`{id,deviceId,kind,title,cwd,status,
  createdAt}`, codex #10: shell/terminal stay ad-hoc). `createSession`/`listSessions`(newest-first,
  device-filter)/`getSession`/`setStatus`(validated)/`removeSession`. Routes (all `requireAuth`):
  `GET /sessions?device=`, `POST /sessions` (validates device∈allowlist + `assertCwd`), `DELETE
  /sessions/:id` (SIGTERM the child's process group via `agentChildren` map, then drop metadata).
  Aliased the imports to avoid colliding with auth.js's PIN `createSession`. +5 node:test (43 total);
  route smoke verified: 201 create / list / 204 delete / 400 bad-cwd / 404 unknown / 401 unauth.
  Device health gate folds into the New-Agent flow (T3).
