# Commit Log — 2026-07-01 · release 0.4.0

- `chore(release): bump to 0.4.0 (sprint-2 merged to develop)`
  PR #4 (Sprint 2 — GET /devices probe + Machines tab) merged to develop. VERSION
  0.3.0 → 0.4.0 + hub/package.json + frontend/package.json (bump-on-merge rule).
  Next: Sprint 3 — 터미널 릴레이 (Device plane core: phone → hub → `tailscale ssh` →
  tmux over WS /pty), the path proven in the infra spike.
