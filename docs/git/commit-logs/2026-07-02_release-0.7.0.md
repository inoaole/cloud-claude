# Commit Log — 2026-07-02 · release 0.7.0

- `chore(release): bump to 0.7.0 (Sprint 5 v1a collector→ingest→timeline merged)`
  PR #8 merged the Hub plane's first data pipeline to `develop`. Minor bump (completed sprint).
  `VERSION` + `hub/package.json` (+ lock) → 0.7.0; `shared/` and `collector/` packages already 0.7.0.
  What shipped: macOS collector (launchd, commit_seen + heartbeat) → hub `POST /ingest` (per-device
  token, idempotent INSERT OR IGNORE) → better-sqlite3 WAL timeline → `GET /timeline` (PIN).
  Next: **v1b** = `session_observed` after a ps/lsof process-scan spike, then **Sprint 6** (Today
  evening reflection PWA reading the timeline).
