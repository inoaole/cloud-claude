# Commit Log — 2026-07-02 · release 0.8.0

- `chore(release): bump to 0.8.0 (Sprint 6 Today evening reflection merged)`
  PR #9 merged the evening-reflection loop to `develop` — the Hub plane's first product surface
  (GET /rollup on-read derivation, POST /note one line/day, Today screen with honest status
  language). Minor bump (completed sprint). VERSION + hub/package.json (+lock) → 0.8.0.
  Next: deploy to the hub (frontend dist swap + hub/src + restart), then the 7-evening habit
  test. Sprint 7 carries the retention pair (day_rollups + purge) + QA/폴리시.
